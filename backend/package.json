{
  "name": "sbg-tracker-backend",
  "version": "1.0.0",
  "description": "Express + SQLite backend for the SBG Preconstruction Bid Tracker.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js",
    "lint": "eslint src/ test/ || true",
    "migrate": "node src/migrate.js",
    "seed": "node src/seed.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "morgan": "^1.10.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "eslint": "^9.10.0",
    "supertest": "^7.0.0"
  },
  "license": "UNLICENSED"
}